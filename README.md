# LACE

## Installation

```
mkdir -p ~/venv-environments/lace
python3 -m venv ~/venv-environments/lace
source ~/venv-environments/lace/bin/activate
pip install numpy matplotlib seaborn jupyter
pip install --no-binary orange3==3.15.0 numpy matplotlib seaborn jupyter
```
